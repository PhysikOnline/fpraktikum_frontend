export enum GRADUATION {
  BA = 'BACHELOR',
  MA = 'MASTER',
  LA = 'TEACHING',
}

export const CONFIG = {
  graduationMap: {
    BA: GRADUATION.BA,
    MA: GRADUATION.MA,
    LA: GRADUATION.LA,
  },
  version: '1.0',
};
