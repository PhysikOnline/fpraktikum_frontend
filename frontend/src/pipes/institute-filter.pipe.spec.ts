import { InstituteFilterPipe } from './institute-filter.pipe';

describe('InstituteFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new InstituteFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
